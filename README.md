# Task-Management-CLI
Here is a complete Task Management CLI (Command Line Interface) written in Python. It is a classic "CRUD" (Create, Read, Update, Delete) application that looks great in a portfolio because it uses JSON for data persistence and includes a professional README.md.
